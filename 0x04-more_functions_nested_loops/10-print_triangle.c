#include "holberton.h"

/**
 * print_triangle - prints triangles of n size
 * @size: the size of the triangle
 * Return: always 0
 */
void print_triangle(int size)
{
	int a;
	int b;

	if (size <= 0)
        _putchar(\n)
    
	for (a = 1; a <= n; a++)
	{
		for (b = 1; b < a; b++)
			_putchar(' ');
		_putchar('\\');
		_putchar('\n');
	}
}
